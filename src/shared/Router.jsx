import { Route, Routes, BrowserRouter } from "react-router-dom"

const Router = () => {
  return (  
    <BrowserRouter>
      <Routes>

      </Routes>
    </BrowserRouter>
  )
}

export default Router;