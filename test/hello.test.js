describe("테스트 그룹 이름(메소드명/변수명/대상)", () => {
  it("테스트 이름(기대 행동)", () => {
    expect(1).toBe(1);
  });
  it("테스트 이름1(기대 행동)", () => {
    expect(1).toBe(1);
  });
  it("테스트 이름2(기대 행동)", () => {
    expect(1).toBe(1);
  });
  it("테스트 이름3(기대 행동)", () => {
    expect(1).toBe(1);
  });
});
